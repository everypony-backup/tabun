import React from 'react';
import ImageUpload from './subcomponents';

export default function (props) {
    return <ImageUpload
        handleChange={(e) => {
            debugger;
        }}
    />
}
